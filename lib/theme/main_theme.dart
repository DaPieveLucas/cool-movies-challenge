import 'package:flutter/material.dart';

import 'app_colors.dart';

class MainTheme {
  MainTheme._();

  static final standardTheme = ThemeData(
    primaryColor: AppColors.primary,
    backgroundColor: GrayScale.gray400,
  );
}
